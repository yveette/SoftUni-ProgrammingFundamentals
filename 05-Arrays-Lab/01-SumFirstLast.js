function sumElements(arr) {
    let first = arr[0];
    let last = arr[arr.length - 1];
    console.log(first + last);
}

sumElements([20, 30, 40]); //60
sumElements([10, 17, 22, 33]); // 43
sumElements([11, 58, 69]); // 80